---
description: Build a feature end to end: issue, worktree, implement, validate, PR, QA handoff
---

Invoke the `build` skill from the dev-workflow plugin and follow it for this request: $ARGUMENTS
